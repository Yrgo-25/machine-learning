/**
 * @file Test suite for the dense layer stub, dense layer and neural network implementations.
 */
#include "yrgo/test/test.h"

/**
 * @brief Run the test suite.
 *
 * @return 0 on success, or -1 on failure.
 */
int main() { return yrgo::test::runAllTests() ? 0 : -1; }
